# 任务清单

阶段依赖与退出条件见 [plan.md](plan.md)，每项勾选须有 [evidence.md](evidence.md) 中的命令、结果和产物依据。R0、T102/T105/T106 已验收合入；当前执行 R2 T103/T104。范例入口和受控回归已实现，live 模型配置缺失，任务保持未勾选。

## 已完成：文档与 Roadmap

- [x] T001 记录用户确认的 LangGraph → DSH 架构、外部优先及 CodeAgent CLI 后置边界。
- [x] T002 更新架构、公共开发 SOP、角色教程及开发状态。
- [x] T003 建立本变更包，列出与现行 Pi、固定项目场景及业务契约的差异。
- [x] T004 完成 Spec、文档链接和差异检查，将实测结果记录到 evidence.md。
- [x] T005 全库复核文档同步，补齐开发、运行、前台/API/用例与验收规范的方向提示。
- [x] T006 在现有文档内拆解可执行 Roadmap、逐阶段验收和证据记录方式，下一步切换为执行开发计划。

## R0 开工与基线核实

- [x] T100 检查开发环境与现有回归；固定 CPU 模块源码版本、公开接口、允许生成路径及非空参考测试，默认以 DocGen 为示范角色。
- [x] T101 核对仓库锁定 DSH 能力和节点责任，落实配置/旧 Run 保留可读且不跨后端恢复的处置；将实施选择写回 spec-delta.md 和 evidence.md，满足 R0 退出条件。

## R1 收敛公共底座

首批已实现：公共业务接线与预写夹具分离、无 assets 双目录场景、DSH session 关联和取消提交检查。T102 已完成原生工具权限和失败矩阵验收；T105/T106 已完成直接 Pi 依赖移除、默认 DSH 配置和通用 CLI/API 场景迁移。

- [x] T102 同步 baseline/ADR 的受影响条款，实施 LangGraph → DSH 及业务契约、工件/事件关联；验证非法输出、权限、超时、取消与重试（AC-DSHF-002、003 自动化部分）。
- [x] T105 迁出 Pi SDK/依赖和固定项目公共执行器；将项目材料和命令数据化，用两个不同路径/模块场景证明公共接线无项目绑定（AC-DSHF-006）。
- [x] T106 同步 DSH 默认配置、API/Console、运行快照及旧记录读取/拒绝恢复；保持 CodeAgent CLI 后置接口，通过配置与启动回归（AC-DSHF-007）。

## R2 真实角色范例

- [ ] T103 实现 DocGen 真实 DSH 范例运行入口，补齐现有教程中的安装、执行、独立检查与联调命令；真实运行并记录产物，缺凭据时标明未验收。
- [ ] T104 在独立工作区复现范例并修改角色再次运行；通过 AC-DSHF-001～004 后标记底座可用，保留 R1 回归证据。

## R3 七角色开发与联调

- [ ] T200 将七角色各自的输入、职责、输出、权限和验证要求写入既有 `specs/06-agents/` 对应章节；沿用公共 SOP，不新建七份任务书。
- [ ] T210 完成 orchestrator、doc-worker、doc-gen、test-gen 的 DSH 角色开发与逐角色验证（包括范例角色的业务补齐）。
- [ ] T211 完成 code、check、review 的 DSH 角色开发及逐角色验证，接通七节点工件与事件交接，通过 AC-DSHF-008。

## R4 外部第一版闭环

- [ ] T201 与 DEV-011 对齐候选测试在可信参考实现上的验证，执行七角色真实生成、独立测试、归因修订及发布，满足 AC-DSHF-005。
- [ ] T220 验证失败/停止、受控修订、取消/恢复、配置不兼容、重复发布；兑现既有适用的恢复/权限/语言门槛，完成全量回归和教程复现。
- [ ] T300 根据 R0～R4 实际证据同步正式验收、追踪矩阵、开发状态及 evidence.md；R0～R4 全通过才完成 DEV-019，不等待后置 T202。

## 后置跟踪：不计入 DEV-019 完成条件

- [ ] T202 转入 DEV-010，在具备公司环境后核对真实 CLI 版本、协议和权限并验收；容量/部署继续由 DEV-013 管理。
