<!--
Copyright (c) 2026 linlisWorkTeam
SPDX-License-Identifier: MIT
文件功能：交接 PR #41 文档冲突处理、验证及未决输入输出。
-->
# PR #41 文档合并交接

用户要求解决 PR #41 的冲突，以便自行合并。本次在 docs/codeagent-contract-sop 将 main@a79b385 合入原分支 9e66776，逐项合并六份冲突文档。相对 main 只修改文档，保留 #37 的 Domain 功能目录和 #40 的 DocGen 内部 Worker 实现；未回退源码、Schema 或测试，也不自动合并 PR。

保留七种执行身份及每个角色的独立设计；当前为六个外层 Agent，DocWorker 由 DocGen 内部调用。其设计随源码移动到 docs/specs/domainFunction/agents/docGenAgent/subAgents/docWorkerAgent/DocWorkerAgent.md。DocGen 文档接入 workerCount、workerResultRefs、批次失败与复用约定；Orchestrator 按五类外层下游任务描述，索引保留共同协议。同步 Status、开发指南、报告图与路径、文件清单。

CodeAgent IO-03～06 已确认但待实现，知识卡片包含接口、C/C++ 目标、项目配置及冻结读取边界不变；TestGen IO-02 输入、预期依据和用例复用仍在确认中。DocGen 的结构回归与组合 Fixture 不等于业务语义或真实模型质量验收。下一次从 TestGen 的独立文档继续逐条讨论，不替用户确定测试依据。

合并内容已通过 npm run typecheck、npm run validate:specs（17 schemas / 7 commands / 8 results / 52 p0）与 npm run test:architecture（1 项通过），文档本地链接核对通过。纯文档冲突未重跑全量 npm test 或真实业务模型；main 原有全量测试限制仍保留在 Status 的历史证据中，不能把旧结果称为本轮通过。最终提交前另核对 diff、清单和三篇交接。

两条分支带入的旧交接先写入 HistoryEpitaph 并保留固定提交链接，再归档；最终保留 1038 CodeAgent 确认、1046 内部 Worker 和本篇。PR 的最终提交及可合并状态以 GitHub 当前状态为准。
