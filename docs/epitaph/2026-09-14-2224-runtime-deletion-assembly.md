# 删除存储组合与目录边界已验证

目标active，/tmp/domain-knowledge-workbench。线上仍d869e1a，未部署、未删除用户数据、未调用模型，PR50 Draft，38/50不合并。

新增SqliteRuntimeDeletions组合SqliteDeletionRows、SqliteGraphDeletion、CAS及PublishedDeletionFiles，接统一SqliteBatchDeletions。调用方必须先持有维护排他再创建会话，传已打开数据库、固定产物根和运行状态读取；句柄由调用方关闭。快照用于首次冻结，恢复无需新库存。输入根配置复制，sourceRoots回调逐次重验。

DeletionDirectoryScope只接受服务器授权根，按canonical路径禁止与源码双向重叠、禁止产物根互相重叠；不存在源码路径按真实父目录保护。每次读取/捕获/移除/清理前检查；历史收据不能自动变成新授权根。不创建目录。实际Composition接线未做，不应把这次组合验证说成线上可删。

RuntimeDeletionTests.log33/33：新目录边界及真实SqliteWorkbenchBatches组合测试，加既有统一应用、图、文件恢复、发布文件、进程锁、架构。确认false拒绝，预览后源码范围覆盖产物根时无意图且批次保留；合法删除保留命令和序号，journal重开重试回执不变。RuntimeDeletionTypes.log与RuntimeDeletionSpecs.log通过。这里只以取消批次验证真实组合；图及文件删除细节由各参与者既有测试覆盖，未声称完整真实项目删除验收。

下一步生产Composition的维护会话中打开固定registry/workbench/publications/graph/journal数据库，组装上述factory再调用BatchDeletions，最后关闭额外连接。journal不要在进入维护前新建空文件，否则needsRecovery会正确拒绝普通入口。源码保护需包括projectStore全部历史directory及旧场景来源；用户变更过的历史发布目录必须明确授权，不可直接采用receipt。当前factory roots要求目录存在，当前publisher/index/CAS构造均创建其固定根。

其他执行目录(agent-workspaces、workbench-audit、demo、codeagent sessions、evaluationArtifactsDirectory)的归属清理仍未完成；不能据已覆盖CAS/发布文件开放不完整删除。随后HTTP预览/二次确认/恢复和UI，恢复后队列重启及部署回归。C/C++修订后来源质量、全部门禁和最终发布仍未完成，无新真实运行。
