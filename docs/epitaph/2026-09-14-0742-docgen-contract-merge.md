# DocGen章节契约与main汇总合并

继续0738的同一main合并，当前49个未解决文件（本轮55减6），无merge commit/推送/部署。目标active，本轮progress。不要abort或覆盖已解决内容。PR50草稿、旧ec72线上、C/C++真实来源复核等未完状态不变。

DocGenRevision保留工作台revisionScope/validateRevision(base,revised,allowed)/outline及markdownSections助手；main同名不同签名函数重命名validateRevisionInput，保留baseBody/correctionTarget/validateRevisionOutput。DocGenContract同时保留Outline/DocumentBody和main Output(含keywords)/SplitProposal/Worker端口。显式executionContract=section-doc-v1启用工作台章节模式，在main纠正验证前先校验精确H2与证据，错误范围不能进入模型。默认主线仍内部Worker+完整文档/拆分决策，默认后续轮次缺base照常拒绝。

DocGenSectionGeneration.ts从原DocGen提取概要/正文/精确修订流程，保持StageValidation日志、预算、输出约束，不另增Agent。section模式输出keywords来自冻结正文H2；原文正文CAS不重新加YAML头，索引生成YAML；main默认仍renderKnowledgeDocument。双方共用Worker完整性校验和候选payload，knowledgeRisks保留。section模式已由assembleSections校验，不重复调用main全文YAML修订比较或模型schema，避免原文头与裁剪正文冲突。

WorkbenchGeneration、WorkbenchCardRevision都显式传section-doc-v1。DocGen Prompt同时含单卡片范围和内部汇总载荷。AgentCommand.schema.json局部合并main orchestrator/docgen/check definitions（docgen新增executionContract），保留前轮Code oneOf；testgen/review尚有冲突，整个schema未stage。不要整文件checkout任意一边。

测试：默认DocGenAgent.test.ts和两个默认examples恢复main版本；原工作台15项角色用例完整迁到WorkbenchDocGen.test.ts及WorkbenchDocGenSample.json，payload带显式版本与workerCount0；首次生成测试删除base后iteration设0，保留main后续轮次必须base规则。测试helper roleExample增加可选样例路径，默认行为不变。临时隔离helper仍只加载DocGen定义绕开未合并Registry。第一次28测19通过9失败，定位初始场景iteration与错误校验顺序；修后/tmp/WorkbenchMergeDocRolesFinal.log 28/28通过，无省略断言。/tmp/WorkbenchMergeDocRevision.log正式章节修订测试通过。本轮staged相关diff --check通过，未跑全树类型/浏览器。无活跃测试或模型进程。

下一步Review契约必须兼容工作台source-review和精确allowedKnowledgePaths，不能直接覆盖为main comparisonReportRef后丢源验证。DocWorker迁移还需main内部worker与旧KnowledgeRisks并存；TestGen可信用例策略/TS回归、AutomatedProjectWorkflow回放保护/绑定和generation版本继续。解决全部冲突再整体验证Schemas、fixtures、Spec及类型和行为。DocGen新增Spec说明当前追加在原七节后，可在文档整体冲突合并时归入对应章节。History仍冲突，新增墓志铭临时超3，统一归档再清理，不删除未归档记录。
