# 发布设置折叠、导航改名与删除应用契约

目标active，本轮progress。最新用户明确：知识发布设置参考模型服务配置采用同一UI/UX；飞轮批次改名“知识飞轮管理”、知识导航改名“知识治理”。已实现默认折叠、展开自动读取、移除查看发布设置按钮、保留标题右侧悬停问号，导航/H1/返回按钮及相关测试同步改名。读失败可重试；离开页面后不写入旧容器。截图人工查看后更新两个已有发布基线，阈值不变。首轮4过2截图旧基线失败，更新后相关7/7通过（/tmp/PublicationFoldBrowser2.log），Spec通过，类型日志/tmp/PublicationFoldTypes.log。这些前台改动尚未部署，当前站点仍dab50a5。

d52d8a2新增BatchDeletion领域规则、BatchDeletionPorts、BatchDeletions应用服务，及6个单元测试。预览规范化图摘要，确认事务内重新读图并拒绝活动/变化/不完整输入，保护共享来源配置；同确认幂等，清理失败CLEANUP_PENDING，重启按回执继续。/tmp/DeletionApplicationTests2.log 6/6，类型/Spec通过。只有接口和替身存储证明用例，没有SQLite扫描/清理适配器、HTTP或前台删除按钮，不能宣称用户可以删除。没有删除线上数据。下一步必须接完整所有权引用图、跨库原子或日志恢复、CAS/索引/发布文件清理与二次确认UI；保留编号序列、累计预算及必要审计，保护共享证据。不要通过仅隐藏列表替代删除衍生产物。

网站dab50a5在2012记录已CI619/41/25且公网验证，PID113062，原tunnel和8run/1card保留。本轮未重新访问线上。bf7aa3f的视口导航状态修复也未部署。下次发布请新发行、独立依赖、精确CI、核对PID/活动任务、symlinks=True备份；/tmp/DeployWorkbenchDab.py硬编码旧PID不可复用。剩余C/C++真实来源及发布门禁、独立阶段与模块批次的统一归属/调度、最终PR38/50比较合并均未完成。原用户工作树不动，无主动搜索和放宽门禁。
