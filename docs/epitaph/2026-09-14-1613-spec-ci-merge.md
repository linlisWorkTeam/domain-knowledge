# CI与当前Spec合并

继续1610同一未提交main合并，goal active，本轮progress。17个冲突减至2个：docs/Status.md与docs/HistoryEpitaph.md。未merge commit/推送/部署/真实模型/PR38操作。原用户树和无关服务不动。

CI合并保留main VerificationRouter的runtime/web/acceptance范围及独立验收job。将feature隔离安装旧docs-only条件更新runtime；verify与acceptance均ubuntu24.04并安装/探测bwrap，临时runner只给bwrap配置userns的AppArmor profile，不关闭全局内核限制。没有在本服务器执行apt/AppArmor步骤。中文字体仅web安装，失败截图仅web上传。保留acceptance只fetch DocGen两个固定SHA，无全历史fetch。YAML解析成功；/tmp/WorkbenchMergedVerificationRouter.log 5/5通过，未声称GitHub远程CI已通过。

CodeTaste/Architecture/DDD/Development/Verification合并当前扁平Domain功能目录与main验证治理；保留feature外层经AgentExecutionService、Domain角色选择与Application持久化边界，以及开发subagent约定和NFR013 Partial（本任务串行）。Verification采用main移除旧路径/文案永久测试的映射，保留AC-DEV-001与对应追踪，不把未运行验收改成已验收。

Application合并工作台阶段、发布/材料用例与main内部Worker、native测试缓存、幂等路由及人工交接；subagent-v4、显式moduleContract协议及执行v11同步。逻辑Worker默认3任务槽与ECS重进程串行区分。Infrastructure evaluation同时保留TypeScript moduleSuite隔离和main原生监督测试源码模式，明确moduleSuite/testSuite互斥。AgentAdapters保留真实原生session（仅opencode.ai）、SSE累计用量、显式生成探针及read_material边界；其他host不发session，旧配置不跨版本恢复。

Agents索引移除重复且已过期的六类tasks/外层Worker/旧Check表，具体默认与显式协议指向已合并角色Spec；保留阶段审计/恢复、H2范围、知识风险OPEN/VERIFIED/OUT_OF_SCOPE及可信证据边界，旧v3/v4/v5现状描述更新为当前v11而非改历史证据。Views4Plus1联合工作台阶段图和main内部Worker/native图；GettingStarted/spec索引路径更新。

旧services/workflow Spec已迁入现有domainFunction/workflow：main流程/回放规则保留，补充AgentExecutionService及TS冻结门禁/H2/风险语义，旧文件删除；通用单文档关联延期与五阶段工作台关联目标明确区分。FileCatalog采用main仅登记无内嵌元数据文件的格式，合入feature仍存在且metadataLocation=catalog项，合法JSON且不恢复全仓库header清单。

Spec lint已越过JSON冲突，现在真实失败为当前文档坏链接 ../src/domain/services/workspace/LocalAgentWorkspace.ts（log /tmp/WorkbenchMergedSpecs.log，报文文件名被脚本裁成ions.md，需rg定位）。接下来修当前指南/Spec内的搬迁链接，不全局重写历史引用；Status/History仍冲突需保留v0.2.0历史与main路线图，新增当前工作台真实未完成状态。History归档要固定提交原文可审计；多轮未提交epitaph不能凭空链接不存在SHA。完整Spec/架构/测试/Console/真实C++验收及部署后才处理PR38/50。定向diff --check通过，无本任务活跃执行进程。
