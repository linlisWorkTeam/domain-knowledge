# Worker/TestGen接入与真实Registry回归

继续0745同一未提交main合并，当前33个未解决文件（本轮45减12）。原备份/工作树边界不变，无merge commit/推送/部署/真实模型运行。goal active，本轮progress。PR50草稿，最后才按条件处理PR38/50。旧ec72线上及C/C++真实验收缺项见更早记录。

DocWorker已迁入docGenAgent/subAgents/docWorkerAgent，移除原根UD契约但保留原事实能力为SourceFactsContract/Prompt/Execution，显式source-facts-v1；默认main仍fragment/analysisScope/sourceEvidence/unresolvedQuestions。统一DocWorkerAgent入口分发，不增加外层Agent。DocWorkerExecutionService从section-doc-v1父任务传source-facts-v1，子任务key升subagent-v4；原生成键和全部其他contract-v11仍待应用整体统一。原事实测试/样例迁SourceFacts.test.ts/SourceFactsSample.json，保留全部断言。ProjectWorkflowFixture局部导入已换SourceFactsContract，其他冲突未解，不可整文件覆盖。

发现并修复实际交接问题：collectRisks原来忽略CAS JSON字符串中的风险。现在对象/JSON风险得到相同标识，损坏JSON/字段拒绝；普通非JSON片段保持原语义，main Worker unresolvedQuestions仍通过unresolvedRisks字段交接。DocGen新增内部Worker JSON风险实际汇总测试保留sourceRef/workerResultRefs与验收待办。

TestGen保留main监督式C/C++源码files/cases和validatedOutput复用；显式behavior-cases-v1走BehaviorCasesContract/Prompt/Execution，支持native和TypeScript声明式用例。两种输入/validatedOutput不可混用。WorkbenchEvaluation传新版本；NativeCases测试选择显式BehaviorCasesSample。新工作台必须有module/native行为契约，不能回退legacy candidateCommands；旧outputSchema保留读历史，但新执行不接受命令候选。TypeScript suite明确oracleRequired=false保持TEST_ORACLE_REQUIRED；native由schema返回AGENT_OUTPUT_INVALID，原断言均保留。示例换成明确PENDING候选（只证明协议，未声称真实参考通过）。AutomatedProjectWorkflow局部test-gen分支已分开TS裁剪roleScenarioRef与main native源码测试；该文件其余冲突很多，未stage。前轮Code/DocGen/Review局部改动都要保留。

AgentCommand.schema.json全部冲突解决且已stage：default main contracts +明确workbench模式；DocWorker executionContract可选const，DocGen/Code/Review/TestGen版本字段明确。JsonSchemaAgentContractValidator成功编译，并校验7默认+4工作台样例，/tmp/WorkbenchMergedCommandSchemas.json全11通过（未覆盖workbench Code命令样例）。AgentResult之前保留main+knowledgeRisks。

ProjectFlow.ts唯一接口冲突合并双方字段（moduleContract/fixedSuite及modules/agentConfiguration/comparisonRules/businessGoal），已stage。Graph.ts保留trackNode、预算deadline/取消检查和完成后投影，renderedNodeId采用main nodeId（DocWorker不再外层节点）；原executionNodeId助手已被main删除，不能引用它。两文件Node --check通过，尚未证明应用/图回归。

验证：Worker定向17/17、KnowledgeRisks6/6、DocGen新交接1/1，分别/tmp/WorkbenchMergeWorkerRoles.log、WorkerRisks.log、WorkerRiskHandoff.log。依赖check READY。关键进展：不再使用隔离helper，实际Registry下src/domain/agents的14测试文件116/116通过，manifest/tmp/WorkbenchMergedRolesManifest.json、log/tmp/WorkbenchMergedRoles.log，9.65秒。之后TestGen禁legacy策略有一次16/17（oracle错误码），修复后17/17 /tmp/WorkbenchMergedTestGenFinal2.log，随后native错误码进一步限定suite，native targeted通过/tmp/WorkbenchMergedNativeRoleFinal.log。未重复宣称116覆盖最后变动。

真实编译定向NativeCases全6测首轮4通过2失败：C宿主比较/错误参考预期、sanitizer参考UB、C++输出参数uint64、DSL注入/越界4项通过；第5项oracle错误码已修并targeted通过；第6项持久化oracle缓存因导入AgentExample.ts未解决marker而未执行，不能计通过。日志/tmp/WorkbenchMergedNativeCases.log。该测试原断言未放宽。最后无本任务活跃进程。可用内存最近766MiB，磁盘3GiB，无swap，未删除数据。

剩余33主要：17项docs/CI/Spec冲突，应用AgentExample/AutomatedProjectWorkflow、ConfiguredProvider、ProjectWorkflowFixture、TrustedProjectEvaluator及相关tests/Fixture。先解决生产应用/缓存/回放和新版本绑定，再全树typecheck/Spec/安全及Console/TS回归。不要将角色层116或编译4项当作工作台C/C++真实目标完整验收。Provider session header仍需对照双方测试和旧实际运行决定；不得丢quota/stream诊断或取消语义。墓志铭目前超3因History冲突，归档再清理，不能丢未提交交接证据。
