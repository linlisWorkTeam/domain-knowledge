# 补证目标检查基础已实现，完整目标仍继续

实际工作树/tmp/domain-knowledge-workbench，基线3b83ac6。上一轮部署重检得到health正常/首页200/公开JS等于v17，属于已取得验证证据；本轮按持续目标继续实现，未重部署或宣称全范围完成。先读1535墓志铭并确认Git，原未提交部署记录/历史归档保留并本次提交。

/tmp/WorkbenchV17FullRegression.log现已终态：494/494，0失败/取消/跳过，309627ms，对应3b83ac6基线；不要再等待89562或旧PID。真实补证stage-f1c54a34c6606ae85d8bae92409c563fd8457887229125c4fab321c839608281之前已FAILED/TEST_CANDIDATE_REJECTED，29候选2参考失败、旧31不变，6个需求章节零命中。证据/root/projects/domain-knowledge-releases/2026-09-10-workbench-progress/real-knowledge-revision/supplement-after-selection-v1/CandidateRejectionAudit.json，任务输出Evaluation.json。source51992已SUCCEEDED/UNRESOLVED，不能重启旧任务或称发布通过。

新增Domain NativeSupplementTargets.ts：显式native-supplement-targets-v1，规范化目标集合，精确引用命中、未命中、case映射、semanticCoverageProven=false；拒绝不合法冻结策略。NativeSuiteEvaluation的supplement新增可选targets进入缓存身份，零命中保存候选与覆盖checkpoint，空oracle且REJECTED，不运行候选。普通/旧输入未注入新规则；历史可信期望不变。来源语义/固定门禁不放宽。

测试：新增3领域测试；原NativeSupplement集成保留全部断言并增加零命中不执行候选、不同策略不复用旧成功、部分命中保留剩余缺口和旧可信不变。当前6定向测试、typecheck、validate:specs、diff check全部通过。494全回归是此前基线，不覆盖此次新代码。重资源未启动，当前无真实模型或浏览器任务。

下一步必须接WorkbenchEvaluation.prepare冻结targets策略、evaluate校验冻结策略，按模块传targets；拒绝报告/下一轮TestGen传targetCoverage和缺口反馈，成功/缓存也保留目标定位报告。旧冻结任务不跨策略恢复；新的策略身份不能靠重建任务重置累计预算，先检查现有预算继承规则。再接页面覆盖详情、受控恢复/预算测试、真实补证重跑；不要把当前可选服务支持当线上已启用。原完整jsmn与TinyXML2闭环、当前完整Console/MarkdownLite回归及最终验收/再部署仍未完。部署dc3dbf8保持原数据及回退，不在未验证阶段提前替换。
