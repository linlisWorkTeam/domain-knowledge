# TypeScript完整模块回归恢复

继续1607同一未提交main合并，goal active，本轮progress。PR50仍草稿，未merge commit/推送/部署/真实模型；用户原工作树不动。剩余17冲突均docs/Spec/CI，ComponentLayout按main删除，旧文件布局/文案/源码字符串检查已废弃，不重新引入。此前Architecture/Site边界检查保留。History冲突期间交接尚未归档，不删除未归档记录。

修复ModuleFlywheel回归：测试装配内部workerRuntime（冻结prompts、observer、ConcurrentTasks），不再依赖已删除外层DocWorker节点。AutomatedProjectWorkflow对明确配置moduleContract的TypeScript模块采用section-doc-v1 DocGen；内部Worker自动source-facts-v1，原源码风险、stage feedback、固定H2修订能力保留。未配置moduleContract的通用生成及C++默认main DocGen协议不变。

相同模块Review显式workbench-review-v1，criteria绑定文档风险与评测，Check报告作为证据；输出correction转带可信evidenceRefs的corrections，DocGen据此定点修订。门禁恢复recommendation ITERATE/unresolvedRisks阻塞，默认main Review仍原blocking/list协议。recordGovernance根据两种Review输出形成可读交接摘要。Check采用新main两侧源码报告契约，若模块未显式提供comparisonRules，则根据冻结moduleContract.signature构造公开接口/行为比较规则。没有向Code泄漏参考/固定测试。

ProjectWorkflowFixture按显式Review模式返回单correction或默认main corrections。ModuleFlywheel旧contract-v5审计键更新v11，所有行为、安全、失败输入、证据引用、固定测试、逐字未授权区段断言保留。DocGen/Review现有Spec第二节同步模块适用方式。

验证：第一轮仅Worker修复后到CHECK_RULES_REQUIRED；补规则后/tmp/WorkbenchMergedModuleFlow2.log真实隔离TypeScript完整7/7，58.2秒：正常发布、同轮range/heading反馈、两轮H2修订、错误候选不得晋升、预算STOPPED、未知来源阻止发布、跨修订风险OPEN到VERIFIED审计全部通过。之后治理摘要适配变动，以/tmp/WorkbenchMergedModuleNativeRegression.log定向3/3复验C++两个泛型两轮场景+TS停止流程，17.7秒。全树npm run typecheck /tmp/WorkbenchMergedModuleTypes.log exit0。定向git diff --check通过。所有session终态，无本任务活跃模型/编译。

下一步17冲突：优先CI与验证Spec，main使用VerificationRouter、runtime/web/acceptance等输出，feature isolation步骤还误引用docs-only已删除输出；应按实际scope安装/验证bwrap，不关闭隔离，不增加未经批准的广泛CI。acceptance历史源码SHA需要正确fetch，UI需要中文字体；不要只选一侧丢安全隔离或重新扩大docs-only检查。再处理其他Spec/文档路径与History归档、完整回归/前台/真实jsmn+TinyXML2新v11链/网站部署，最后按用户条件处理PR38/50。上述受控C++不是TinyXML2真实供应商最终验收，旧source质量未解决事项仍见早前交接。
