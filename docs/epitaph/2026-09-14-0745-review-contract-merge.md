# Review来源与比较两类契约合并

同一main合并当前45个未解决文件（本轮49减4），未merge commit/推送/部署，无真实模型任务运行。目标仍active，本轮progress。保留所有前轮局部解决内容和backup分支；不能abort或覆盖原/root/projects/domain-knowledge-wxc。真实验收与最终PR38/50条件不变。

ReviewAgent.ts现在为统一入口：默认main comparisonReportRef模式输出corrections列表，明确workbench-review-v1走WorkbenchReviewExecution。后者沿用原validatedStage、sourceReviewTimeoutMs、单个correction和recommendation/风险规则。WorkbenchReviewContract/Prompt保留精确H2动态Schema、allowedKnowledgePaths、PASS矛盾拒绝及有限修正。两种输入拒绝混用；未带版本的旧criteriaRef输入不能静默升级。模型不自行提供CAS证据，两种payload都从可信材料绑定。

领域knowledge/{SourcePublication,SourceRevision,KnowledgeSourceVerification,KnowledgeRevision}和Workbench相关服务Output类型改为WorkbenchReviewContract。WorkbenchKnowledgeRevision/CardRevision/SourceVerification实际Review调用已加workbench-review-v1。原Review测试和sample保留main，新增WorkbenchReview.test.ts/WorkbenchReviewSample.json承接原10项测试，原测试断言完整保留。SourceReviewPolicy和ReviewScope使用显式工作台类型/样例。Review、DocGen新增Spec说明已移入原第二节，维持七节结构。

AgentCommand.schema.json的reviewPayload局部解决为main/default和显式workbench oneOf；目前只剩testgenPayload一个冲突块，仍未stage整个schema。保留此前Code/DocGen/Check/Orchestrator部分，不能整文件checkout。首次替换脚本因尾部边界断言失败，未写入schema；随后按实际尾部结构完成替换，未丢额外定义。

验证：正式ReviewScope+KnowledgeRevision 6/6通过/tmp/WorkbenchMergeReviewScope.log。临时helper隔离Registry后main Review、workbench Review、SourceReviewPolicy共18测首轮17通过，失败是工作台测试buildPrompt仍引用主线路径；修正导入后/tmp/WorkbenchMergeReviewRolesFinal.log18/18。其他断言未省略。相关staged diff --check通过。此证据不是整站或完整Registry回归，未部署。无本任务活跃进程。

下一步DocWorker：main迁移到docGenAgent/subAgents/docWorkerAgent，原根DocWorkerContract仍UD，内容是原事实/风险/精确源码校验；当前新路径main Contract定义fragment、analysisScope、sourceEvidence、unresolvedQuestions。不能直接删除原事实验证来消冲突；需保留其授权证据保护并接入内部Worker。DocWorkerExecutionService主线消费JSON片段与unresolvedRisks。TestGen亦有main可执行测试和工作台Native/TS案例协议冲突，需要显式衔接。之后合并AutomatedProjectWorkflow/main回放保护与stage日志、统一contract-v11、修Schemas/fixtures/Spec、全量类型和行为验证。墓志铭临时超3，History仍冲突；先归档再清理。
