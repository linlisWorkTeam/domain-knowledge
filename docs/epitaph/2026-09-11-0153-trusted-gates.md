<!--
Copyright (c) 2026 linlisWorkTeam
SPDX-License-Identifier: MIT
文件功能：交接跨输入版本可信测试门禁继承修复。
-->
# 可信门禁跨源码和工具链变化保留

工作树 /tmp/domain-knowledge-workbench，分支feat/five-stage-workbench，前序6e5e6d9。上一轮与本轮均实际进展，goal继续active；完整工作台仍未完成，不标记complete或blocked。本轮所有测试进程terminal，没有运行中的模型或原生验收。线上taste/4310、v0.2.0、原wxc其他改动保留；未清理旧知识。

新增Domain NativeTrustedGates，契约native-trusted-gates-v1。SQLite NativeTestStore.lineage按稳定卡片集合读取所有TRUSTED历史记录，含旧无gateDigest记录。Application逐套验证CAS/oracle/原绑定，再合并原始用例，完全相同内容去重，同名不同用例给确定性别名并保留不同预期。新cacheKey绑定gateDigest；referenceKey仍管理当前参考分支事务父链。新记录inheritedTestSetIds记录来源，旧记录不改。未变输入仍复用；工具链、源码、策略或正文变化都重验原门禁，不再调用TestGen替换旧预期。后来补入的历史可信套件会使旧成功缓存失效。公开接口摘要变化时保守PAUSED NATIVE_TRUSTED_INTERFACE_CHANGED；并集超过64例时PAUSED NATIVE_TRUSTED_GATE_LIMIT，不删减。

历史门禁在新参考失败保存REJECTED新测试集，但原可信集仍TRUSTED且不可变。WorkbenchEvaluation用独立trusted-gate-conflict检查点/报告，状态TRUSTED_GATE_CONFLICT，阶段PAUSED NATIVE_TRUSTED_REFERENCE_FAILED；恢复再次检查原门禁，不进入candidate-rejection反馈或模型重新提出。UI显示原输入/预期/实际与明确原因，保留恢复/下载。新候选首次参考失败仍走原候选拒绝，不把知识判错。

NativeTrustedGates纳入NativeFingerprint引擎文件清单；本轮变更了执行器指纹。前轮TinyXML2 37/37、jsmn15/15与更旧16/16都仅为相应旧指纹下证据，最终新引擎验收必须重验，不能跨指纹恢复旧阶段。运行目录和固定仓库/报告路径继续见0143交接；未发起本轮真实模型或固定目标重验。预计jsmn同卡片集合会继承旧16与新15套件的并集，不只替换为新15。

验证全部通过：typecheck、Spec、architecture8、domain51、integration196、Console30。NativeCases原proposals=2断言收紧为1：工具链变化不得重新提案；其他断言保留。新增验证源码/策略变化重验、旧套件不变、真实独立oracle通过的旧格式套件补入后不能被成功缓存绕过、冲突同名用例并存、容量停止。WorkbenchEvaluation新增真实C参考行为变化后暂停及同任务恢复不会重新TestGen。证据 /root/projects/domain-knowledge-releases/2026-09-10-workbench-progress/trusted-gates/{Verification.json,Integration.log,Console.log,NativeCases.log,StageConflict.log}。Docs领域/基础设施/UI及Views4Plus1已同步。

下一步优先持久化一键串联及操作中心：复用GENERATE/INDEX/FLYWHEEL/EVALUATE/ASSOCIATE应用入口，独立协调记录不能占用Stage单执行槽等待子阶段（避免死锁），冻结输入/配置、持久化子任务编号、取消传播、停在失败阶段且原预算不重置；成功阶段复用。后续仍缺规范化/结构代码比较、有证据知识修订、指定外部材料关系、固定质量发布门禁、TS统一边界/markdownLite、构建配置实际应用、统一导航、双目标最终新引擎真实验收及部署。公开接口变化适用性目前保守暂停，尚无用户审定或迁移用例流程；不要伪称自动解决。不新增主动搜索、账户、全工具链下载或固定三轮上限。
