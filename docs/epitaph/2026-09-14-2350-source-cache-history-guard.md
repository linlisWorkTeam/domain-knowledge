# 来源缓存和发布恢复拒绝历史格式矛盾

全goal active，/tmp/domain-knowledge-workbench，PR50 Draft未合并，网站仍2c55117，无部署/删除用户数据/新模型调用。C++37+40门禁与已取消来源348de05详情见2345及WorkbenchAcceptance报告。

新assertReviewFormatHistory领域规则按原格式失败输出保留除replacementMarkdown外全部字段。应用assertSourceReviewHistory读StageEvent及已验证CAS，检查事件/尝试stage、attempt、status绑定，按final-source语义键跨taskAttempt分组。WorkbenchSourceVerification在缓存读取前和整卡汇总后调用；WorkbenchSourceFindingHistory继承纠正意见前同样调用；WorkbenchPublicationEvidence.prepare亦调用。WorkbenchPublicationEvidence.verifyResume读取原准备工件，检查四任务input/result摘要和来源历史；WorkbenchPublications.finish导出前调用，旧PREPARED不能绕过。

30项定向测试、typecheck、Spec通过，日志/tmp/SourceHistoryTests.log、SourceHistoryTypes.log、SourceHistorySpecs.log。覆盖跨taskAttempt矛盾PASS导致源缓存读取前拒绝、发布准备及已准备记录verifyResume拒绝，正常真实SQLite/文件发布幂等与中断恢复保持通过。旧断言未放宽。只读/tmp/AuditCurrentSourceHistory.ts审计真实348de05的67事件并拒绝REVIEW_REPAIR_FACTS_CHANGED；保存cpp-current-contract-source/HistoryAudit.json，未改DB/CAS。没有恢复已取消任务。

下一步为来源新执行契约/待核实线索保留制定可执行路径，不能原taskId清检查点或重置用量重跑。当前SOURCE_VERIFICATION_CONTRACT仍v4，未实施v5；复核格式错误的输出只是线索，不能自动晋升为已证明纠正意见。应保留该卡第25行头文件内联说法与固定源码cpp定义的真实差异证据。当前取消任务缓存里有错误SOURCE_MATCHED；现在恢复会拒绝，不能把这个拒绝当作完整验收完成。

其他未完成：旧缺失checkpoint owner的安全恢复、历史多发布目录授权、保留评测目录清理、C最新修订后全门禁、C++来源修订与新版本门禁/发布、线上完整五阶段验收。保护性拒绝不替代目标功能。所有原进程先核对PID/日志后再动；260138已退出，控制器59679/61616已结束，不另起重复来源任务。
