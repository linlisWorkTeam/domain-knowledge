<!--
Copyright (c) 2026 linlisWorkTeam
SPDX-License-Identifier: MIT
文件功能：交接真实修订失败及最终正文源码复核门禁。
-->
# 修订正文源码复核

继续 /tmp/domain-knowledge-workbench、feat/five-stage-workbench，基线 6d3d24098ec2bb4c45ede8e0b78af599db0a1fcf。完整 goal「继续」仍 active，未完成；原 wxc 脏树、taste/4310、v0.2.0 未改。Node24/384MiB，重任务串行，磁盘约170MiB，未删旧知识。无 subagent/skill。

6d3d240 已提交 v5 当前版本冻结：新流程从原生成卡片沿同项目快照稳定卡片血缘选当前头，initialVersionIds 进入新流程身份；恢复不重读当前头。旧 GEN 产物不变。跨快照或非后代拒绝。

本次推进 revision-v4 / pipeline-v8；旧版本只读。修复真实模型 Review→DocGen 交接：从校验过且绑定当前任务/原输出的 AgentResult 取标准化 COR 编号及四个允许字段，不能直接传播模型原始 correction。Review 同时看到固定参考与生成代码，Code/TestGen 仍不可见参考/隐藏测试。授权 H2 先进入 Review 输出 schema 和 prompt，防止角色成功缓存越界意见后 App 永久拒绝。没有 scope 的旧 Review schema 保持不变。去掉 priorCardDecisions 材料，避免未经证实的其他模型意见相互强化。

DocGen 修订完成并确定性装配尾注后，最终正文 CAS 先送独立 Review（phase REVISION_SOURCE_REVIEW），仅固定源码、正文和旧评测；不把上游修订意见作为事实。必须 PASS、无阻塞、无纠正、无风险才能提交候选，metadata 绑定 sourceReviewResultRef。拒绝保留 draftRef/sourceReviewRef 及明确 criterion/risk，返回 UNRESOLVED，不提交草稿版本、不更新草稿索引。现阶段拒绝不会自动二次 DocGen；如需后续反馈，必须明确版本化、保留原拒绝检查点/用量，不能靠重复启动清预算。通过仍 verified=false，不代替最终发布门禁。UI 可下载草稿及复核意见。独立源码 Review 是必要检查，不保证模型事实正确。

真实验收副本 /tmp/workbench-revision-acceptance-20260911，原 runtime /tmp/workbench-native-acceptance-20260910 未改。固定源码 /tmp/workbench-reference-inputs，原 jsmin31/Tiny37可信预期未改。故障注入明确为人工将 jsmn_init pos=0 改为1，首轮 Code 同故障；GEN 仅交接原真实卡片，不是新真实生成。Review/DocGen/后续Code 才走真实 provider。
- pipeline-f9a465b55b3f702ed3d6c2a60e59f4b18d2e45850f270c5ae3ab1fff3cfd8b4e（v5/revision-v1）5/31，Review 原始纠正字段导致 DocGen 命令拒绝，FAILED。
- pipeline-b14a6e52d3549a954b23e09d919abe8e25c8a9d023e3ade2b3071ff75fb7093f（v6/revision-v2）真实 DocGen 正确修复 init 为 kv_7b039171486379cdce0e2979，后续 Review 越界，FAILED，未到索引/新Code。
- pipeline-7ccfae1ff7854731bb30843cdf1954f13c2c514a47c8267ec7bc7147e4c4fe1e（v7/revision-v3）发现模型把原本正确的 parse 卡片改错后主动 CANCELLED；8阶段调用、393702 tokens（含受控首Code，不能全部算真实模型调用）。错误候选 kv_76d70f740868291e0a0849be 将字符串 token end 写成 parser->pos+1；固定 jsmn.h 第216行实际是 parser->pos（SHA c04533e9181e1e33baceb0f55ac449b05145bb936e8c68cc77dfe0d8277514fb）。此候选曾通过格式/质量，未验证、未发布；本次加入独立源码复核正针对这一缺口。副本当前头仍可能包含这个坏候选，不要部署或称真实验收通过，也不要恢复旧 v7 流程。

证据根 /root/projects/domain-knowledge-releases/2026-09-10-workbench-progress/real-knowledge-revision，scope-v3/DetectedReferenceMismatch.json 保留确切源码和坏正文证据；三个 Pipeline/Run 日志保留。/tmp/RunRevisionAcceptanceV4.ts 已准备新 source-review-v4 子目录和独立 stimulus，但尚未启动！它使用副本当前后代（含上述真实模型错误），再明确注入 init 故障，首Code受控，其余真实。不可覆盖旧报告；新运行可能仍被 sourceReview 拒绝，必须如实记录。旧实际任务均 terminal。

验证：typecheck、Spec、架构8、Domain67、integration208通过；Console30通过（初跑22通过1失败，原因是受控Review未处理新增phase，修复样例后原断言重跑通过，剩余7通过）。拒绝集成验证不新增版本/不索引/可信预期保留；接受后索引失败恢复不重复Review或DocGen。日志及Verification.json见进度证据目录 source-review-guard。本轮未新增真实模型请求。

下一步：先用 v8/v4 执行真实修订验收，独立核查模型最终声明（尤其 pos+1 事实），不以格式/相似度/行为通过代替知识真值。必要时将源码复核明确纠正反馈给 DocGen 的有界草稿修订，原失败和预算不重置。最终固定源码测试/发布门禁、TS共同边界与markdownLite最终验收、构建参数实际应用缺项、多项目/旧快照范围、双目标最终运行、浏览器截图及当前网站部署仍未完成。目标保持 active，不修改已发布v0.2.0、不加账户/主动搜索/全语言工具链包。
