# 来源拒绝记录恢复修复与固定参考反证

全goal active；/tmp/domain-knowledge-workbench，PR50 Draft，网站仍2c55117。d65914e CI34865148077全过725代码/44Console/25隔离验收；234919a中文错误说明已推送，CI34866183893仍运行。独立发布目录/root/projects/domain-knowledge-releases/2026-09-15-workbench-234919a/app已准备并Composition导入过，Prepared.json记录未部署，依赖清单不变。剩余磁盘约300MiB，不再重复复制424MiB依赖；当前网页/数据未改变。

真实v5 task stage-434571dde4545602e48f0c9fdc15e7ad1d473f93b1759819825c36f2f024abf7在59调用3891825tokens后FAILED/REVIEW_REPAIR_FACTS_CHANGED，原PID288090退出，等待诊断PID291697也已结束。162检查点/51章节/307直接引用已核对，cpp-source-v5/Resume2Audit.json。故障在ToBool输入边界章节kv_f5387...；cbe817原H3替换失败，95e56后输出改变事实并已REJECTED，不是接受坏PASS。

固定TinyXML2参考诊断已执行成功：/tmp/CppHexPrefixReference.log，cpp-source-v5/HexPrefixReference.json和HexPrefixHarness.cpp，使用原CAS源码与冻结build、NativeToolchain隔离。ToInt对0x/0X/0xg/空白0x均true、0，0x10为16，junk为false、初值123。该结果反驳ToInt章节的错误修订推断，不要把模型CONFIRMED当成真理；未改可信测试预期/未发布。

本轮恢复修复：ExecutionContext.stageJournal.history可选只读跨任务尝试；WorkbenchRoleExecution按同role/key/stage读全部历史并校验事件/工件绑定，read仍当前尝试。Review初始prompt携带原格式失败的全部受保护字段，历史不进入当前计数/时限。assertSourceReviewHistory仅跳过事件和工件同时REJECTED/REVIEW_REPAIR_FACTS_CHANGED的不可用输出；原格式失败和所有PASSED仍约束。37角色/发布/恢复测试+4完整原生集成/type/Spec过，原断言保留。只读/tmp/AuditReviewRecovery.ts核对v5的644事件允许恢复；旧/tmp/AuditCurrentSourceHistory.ts核对v4的67事件仍拒绝，没改历史。

下一步：提交后确认无RUNNING再用/tmp/RunCppSourceV5.ts加同taskId恢复，可写/tmp/CppSourceV5Resume3.log；当前写此记录时尚未启动，必须读实际进程/DB。不要重起另一个v5任务。保留59调用和已完成章节。sourceCorrectionPolicy仍v1，混合未知/明确错误不能直接修订，且错误0x意见需通过正式参考证据进入后续评测，不能删除风险来通过。C/C++最终修订/再门禁/发布、旧删除owner兼容/历史发布目录/保留评测目录、线上完整验收仍未完成。不合并PR、不删用户数据，保留work/ljy/现网站与隧道。
