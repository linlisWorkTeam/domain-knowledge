# 发布前接口与测试策略

/tmp/domain-knowledge-workbench，feat/five-stage-workbench，基线9cc96b63da45b3c1deaf0598f4b6d4b13ee1b58f。原脏区/旧网站不变，未删知识，目标active；Node24/384MiB重任务串行。

prepare(v7)从Code interfaceRef与project模块源路径重建NativeContract，nativeFunctions提取目标，固定/可信suite重跑assertNativeBehaviorSuite。可信interfaceDigest匹配契约摘要，policyDigest按原native-test-policy-v1公式用冻结test-gen prompt/roleExecutionVersion/contracts/provider计算；configurationRef.sha256匹配阶段configurationDigest。知识单元与源码模块仍按sourceModules分开。

16定向测试、8架构、类型/Spec通过。新增policy/interface篡改与无目标调用固定用例拒绝；第一次测试误写错误码NATIVE_TEST_SCHEMA_INVALID，修正为实际NATIVE_BEHAVIOR_SUITE_INVALID后通过，未放宽断言。测试夹具已有真实形状API声明/配置及调用，不跳过schema。

真实只读 /tmp/VerifyPublicationTestPolicies.ts 对C31/CPP37可信suite的接口/策略/格式验证通过；此前项目绑定和原始观察仍通过。无模型/编译调用，脚本退出0。证据publication-test-policy/ActualReports.json、VerifyActualPolicies.ts及日志。

下一步先完成project snapshot身份重算和分析manifest内容与sourceFiles/objectId核对（不要改变旧snapshot身份）；然后应直接推进独立SQLite审计/可恢复Markdown发布事务、Composition/API/UI及一键最终提交。不要继续把已完成的接口/策略/H2/材料绑定拆分重写。PREPARED仍不是VERIFIED，服务尚无正式入口，真实整体来源未通过不能发布。

来源task stage-3ac5a7c55407b750e1205020b04657f09b5e4d4590903f6e5592bf3ab3967b48，PID2411792/session82991再次确认活跃，最新RUNNING，29章节/3卡片，总7卡。driver /tmp/RunWholeSourcePolicyV1.ts，日志 /tmp/RealWholeSourcePolicyV1.log，完整source-policy-v1/WholeSource.json，不重启。慢请求仍按新600秒单次策略运行，总任务累计时间不等于单次期限。

完整剩余目标：来源修订后新重建/可信固定来源复测，双目标新版一键链路，最终发布，Make/CMake模块参数、TS完整分析/历史选择，全回归和网站部署。
