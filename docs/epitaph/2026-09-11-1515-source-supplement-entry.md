# 来源补证入口与真实新版重建

实际/tmp/domain-knowledge-workbench，feat/five-stage-workbench，d58b044bca9f8affd09babd3139d347b81ceec26。目标active，旧站未改，完整双目标/发布/部署未完成。当前权限danger-full-access，不再传sandbox_permissions。

已接通：NativeSupplementDemand领域规则从完整绑定卡片/H2提取UNRESOLVED与原原因，稳定digest；拒绝缺节、错版本及无原因。WorkbenchEvaluation.start/prepare可选sourceVerificationTaskId，验证来源任务完成、同Code/版本/源/配置、原评测digest；冻结supplementRef和knowledge-test-supplement-v1。执行时重算校验CAS，按模块给TestGen传需求、给NativeSuiteEvaluation传需求摘要。operation仍undefined因此沿用可信失败修订和来源用例；Composition拒绝未知supplementContract。HTTP POST native-evaluations可选该字段。Console来源面板显示“补充验证用例”，读取原evaluation的Code任务并回调父评测面板显示新任务/取消恢复；不要求JSON。

验证：新增需求单测2/2、SourceRevisionPanel共2/2通过；原WorkbenchEvaluation三场景实际3/3，/tmp/SourceSupplementApplication.log，29769ms。第三mixed场景末尾新增补证断言，原全部testCalls=2断言保留；新增TestGen精确Limits需求、1新例+1旧例、2通过，重复启动同task、不接受其他Code绑定。类型/Spec/diff通过。随后额外收紧evaluationDigest及section outcome校验，类型/领域和panel仍通过，最后全套需覆盖。未做新HTTP浏览器真实点击及全Console。

下一关键功能是一键自动补证，当前仍v16。src/application/services/WorkbenchPipelines.ts约191行sourceReason后，非pipelineSourceRepairable直接停。可加v17与iteration unknownSections/补证来源记录：先明确纠正；纯unknown调用evaluation.prepare(code.taskId, verified.taskId)，下一轮复用同Code/fixed并重新source；新增行为失败走现有revision。Domain按未知cardId#H2集合是否严格缩小判无进展，不靠随机caseId。冻结/恢复/取消/预算、旧契约只读与fixture/UI版本同步必须覆盖。pipelineSourceRevisionFailure目前对UNRESOLVED结果要求cards全REVISED，新真实c32f里6REVISED+1UNRESOLVED可能仍停，须审查怎样安全推进已保存有效映射，不放行未修复门禁。不要降低最终source发布要求。

真实c32f修订已SUCCEEDED/UNRESOLVED，15calls794250tokens，elapsed212684。6新卡ACCEPTED，一张kv2071保留UNRESOLVED：92248、07e0、786d、7089、2cd4、09cb六新版本；完整7列表在source-correction-selection-v1/SourceCorrection.json。session26127已退出0，不再等待。新Code a2f02d237808a5f6106edde5a47373f7fd3ad28b176c978094607d3f40867805已SUCCEEDED，1call43816tokens28943ms；/tmp/RunReconstructionAfterSelection.ts，log /tmp/ReconstructionAfterSelection.log，输出reconstruction-after-selection-v1/Reconstruction.json。session69117退出0。

现在启动真实可信评测/tmp/RunEvaluationAfterSelection.ts，session53528，/tmp/EvaluationAfterSelection.log；输出evaluation-after-selection-v1/Evaluation.json。刚启动，下一轮确认taskId/PID并poll同handle。当前重资源单任务；待完成后固定评测、完整来源与补证使用新版7卡继续。旧fixed suiteRefs/期望保留。验证配置此前过期已用原接口重验VERIFIED，revision5，无需重复验证。真实编译或模型新增用例补证尚未验收。
