# 模块构建配置贯通

/tmp/domain-knowledge-workbench，feat/five-stage-workbench，实现e00c4509842485bd5f0190094cc11f2e6e42e24a。原工作区/旧部署不变，目标active，Node24/384MiB重任务串行。上一轮为代码与测试实际进展，非等待。

WorkbenchProject可选moduleBuilds完整覆盖，projectModuleBuilds验证只包含所选模块、合并默认参数；moduleBuild读取有效配置，moduleFingerprintKey在存在覆盖时使用module-sha256(moduleId)，否则保留原language键。空覆盖不入snapshot，旧项目身份不变；createProjectSnapshot核对已规范化覆盖。HTTP projects接受moduleBuilds，应用冻结入snapshot身份。

Generation公开接口/DocGen构建材料、Reconstruction工具链/Code材料/生成接口、可信与固定评测、PublicationEvidence原始实现build/fingerprint比较均使用模块配置。Composition流程环境指纹有覆盖则按模块算，无覆盖保留旧语言摘要。构建缓存因snapshot和模块指纹变更失效，旧运行继续原输入。全局project.build剩余引用仅默认helper和legacy环境分支。

RepositoryAnalysis表单新增参数范围，模块初次编辑复制默认参数；可恢复继承，取消选定移除覆盖。保存moduleBuilds，应用编译数据库候选作用于当前参数范围。尚未浏览器验收，实际多模块不同宏编译也未运行（真实模型仍活跃，重任务串行）。不要声称已完成Make/CMake动态配置；当前仍只有compile_commands提取和构建系统识别。

验证13项模块输入/发布准备/项目门禁，以及12项架构/事务/模块输入回归通过（有重叠），类型/Spec/diff通过。新ModuleBuilds.test用真实Git/SQLite项目、2个C模块分别C99/VALUE=1和C17/VALUE=2，验证重建prepare调用不同build并保存不同指纹；snapshot执行器是受控替身，无实际编译。空覆盖不改变legacy snapshot，未知模块/越界路径拒绝。证据real-knowledge-revision/module-builds。后续必须补受控完整执行或真实多模块编译和前台浏览器检查。

真实source task stage-3ac5a7c55407b750e1205020b04657f09b5e4d4590903f6e5592bf3ab3967b48，PID2451866/session24433仍活跃；日志/tmp/RealWholeSourcePolicyV1-attempt2.log。最新35calls/836370tokens/reserved3010773，38章节/4卡（7卡总）。已完卡kv_1312...UNRESOLVED、kv_49fb...SOURCE_MISMATCH、kv_5a349...UNRESOLVED、kv_64a851...UNRESOLVED；不是来源通过，不能发布。等待当前任务结束再根据真实纠正意见执行来源修订/未解决项处理，不启动第二个模型任务。attempt1失败及一次错误ID启动已记录0838。当前已加载进程代码不受磁盘新moduleBuild改动影响。

完整剩余：真实来源修订后新Code/可信固定/source复测，双目标v15一键/分步真实发布；Make/CMake动态参数与依赖识别、TS完整源码分析/公开接口/历史版本选择、模块配置实际执行/浏览器、全回归markdownLite、桌面/窄屏前后截图、最终报告/运行编号与部署。v0.2.0不改写，无账户/主动搜索/向量数据库。不要继续重复已完成发布准备检查。
