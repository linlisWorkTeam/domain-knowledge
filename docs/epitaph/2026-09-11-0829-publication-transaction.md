# 本地发布事务实现

/tmp/domain-knowledge-workbench，feat/five-stage-workbench，基线e8f626c98c77da49c88c7ee1fb7a28dd9e0d60be。原脏区/旧网站未动，未删知识，目标active；Node24/384MiB重任务串行。

新增Domain WorkbenchPublicationRecord(workbench-publication-v1)：identity冻结项目/版本/preparationRef/导出files，稳定cards/cardId.md和manifest/evidence路径。WorkbenchPublications先调用完整prepare，再复用KnowledgeIndexStore.render导出YAML Markdown到CAS，SQLite原子insert PREPARED审计，文件导出校验/原子rename后commit COMMITTED。重复提交/恢复幂等，失败保留PREPARED与lastError；不改旧KnowledgeVersion正文/status/gate。

SqliteWorkbenchPublications独立wb_publications/wb_publication_events；LocalWorkbenchPublicationFiles从CAS生成专属staging目录，文件及目录fsync、全文件摘要检查、rename后验证再允许SQL提交。已有完整目录复用；异常目录/符号链接拒绝，不覆盖未知数据。缺失整个目录可从CAS重新导出；已存在的损坏目录目前明确冲突，非自动修复。

19定向单元/集成、8架构、类型/Spec通过。新集成用真实SQLite/文件/YAML与受控准备输入：重复发布仅1record/2events；导出后模拟中断，重开SQLite恢复COMMITTED，mtime不变、总3events；符号链接替换拒绝。测试不是实际OS断电/真实模型整链路发布。证据publication-transaction。prepare夹具抽到tests/helpers/PublicationPreparationFixture.ts，原断言保留。

下一步必须接Composition/API/UI和pipeline最终发布，不再追加零碎准备检查。接Composition前补WorkbenchPublications关闭生命周期：跟踪prepare阶段和resume的全部Promise，closing拒绝新请求但等待已接收任务完成，避免composition提前关闭SQLite。初始化publicationStore(runtime/workbench.sqlite)、LocalFiles(runtime/publications)、Evidence用现有stages/repo/artifacts/projects/tests/contracts，render复用knowledgeIndex store。API提供发布/查看/恢复/受限文件下载；页面COMMITTED才显示已验证，旧知识门禁保持原样。pipeline升版并把最终发布ID纳入历史，旧流程只读。不要冒用旧Flywheel runId。当前事务服务尚未接入口，也未真实发布知识。

真实来源task stage-3ac5a7c55407b750e1205020b04657f09b5e4d4590903f6e5592bf3ab3967b48，PID2411792/session82991再次确认活跃，最新FAILED，33章节/4卡片，总7卡。driver /tmp/RunWholeSourcePolicyV1.ts，日志 /tmp/RealWholeSourcePolicyV1.log，完整source-policy-v1/WholeSource.json。继续等待原任务，不能启动并行模型/编译重任务。

整体仍需真实来源修订后重建/可信固定来源复测、双目标新版一键链路、发布入口、Make/CMake模块参数、TS完整分析/历史选择、全回归和网站部署。未达到完整目标。
