<!--
Copyright (c) 2026 linlisWorkTeam
SPDX-License-Identifier: MIT
文件功能：交接真实原生运行和模型契约问题。
-->
# jsmn 真实多卡片与重建完成，TestGen 仍未通过

工作树 /tmp/domain-knowledge-workbench，前序42f6e74。线上taste/4310与原wxc工作区未改。goal继续active，有具体进展，无真正阻塞。完整目标和剩余任务见1850交接。

新增 scripts/release/RunNativeWorkbench.ts，读取Targets.json固定提交/源码/测试摘要，以真实配置生产composition顺序生成、索引、重建、评测；支持--resume-task，累计用量不重置，错误不晋升/发布。runtime /tmp/workbench-native-acceptance-20260910 使用从live复制的加密配置和密钥，未输出凭据。报告 /root/projects/domain-knowledge-releases/2026-09-10-workbench-progress/native-live/JsmnAttempt{1,2,3}.{json,log}。三个进程均已终止exit1，没有在跑的验收进程。

真实jsmn生成7张卡片，14次模型调用；GEN task stage-91d4da18c21ed7ce111203a84ad08f0a82e82e1999e292fe2ae3649e2a7afa55。INDEX成功；FLYWHEEL stage-cffcdb4abb8579c22f586fcc6631ffa2cc7e312a20ffec94b0852fcaf972e8da 成功，接口compatible=true ratio1，Code一次真实调用。无行为通过或发布证据。

EVAL stage-d88319f12f5443dcb26147cf0ac4271a5b1de9ed1a062c350bcd1eae7a4a28e2 三次同任务恢复均AGENT_OUTPUT_INVALID，累计modelCalls3/tokens131442/reserved452960/elapsed93848ms。参考空baseline成功，TestGen输出在整个对象关闭后才追加oracleRequired，三次均同样JSON错误。未进入候选参考验证，未改知识或晋升。保留dsh-configured/**/session.jsonl用于定位；不要读取/输出provider配置凭据。Attempt1/2提取JSON只是诊断prefix，不是有效候选。

第三次之前补TestGen原生指导：oracleRequired置前、零初始化结构体不填initial、字符串内联不用指针变量，但第三次仍同样错误。进一步发现providerPrompt把所有properties写成必须恰好包含，误把optional字段描述为必填；现改为声明字段与required分别说明，没有弱化schema。新增适配器回归18/18通过，typecheck和Spec通过（新测试加入前跑类型）。现尚未用修复后adapter发起第四次模型调用。

下一步检查输出约束措辞和schema字段顺序，必要时提供有效顶层骨架，再有依据地恢复EVAL或推进TinyXML2真实链路。不要盲重试相同失败。不能手修模型JSON并冒充候选；错误候选仍须参考oracle拒绝；数组参数应read衰减而不是address整个数组，真实模型仍可能违反。修改指纹涉及的native engine时不得有实时验收进程。后续规范化差异/知识修订/关联/一键持久化/固定门禁/真实双目标与部署均未完。磁盘约269MiB，用户允许空间不够删除旧生成知识，本轮未删。
