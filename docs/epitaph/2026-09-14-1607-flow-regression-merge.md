# 工作流测试合并与 TypeScript 回归入口

继续1604同一main未提交合并，goal active，本轮progress。工作树和权限边界不变，未提交/推送/部署/处理PR38。剩余18个U：17 docs/CI/Spec加ComponentLayout删除冲突。生产src、其余测试已无冲突标记。History未归档，不能删除尚未归档记录。

已解决并stage DshProjectStages、DshConfiguredFlow、AutomatedLanggraphFlow。默认应用DocGen现在采用main完整输出（内部Worker），测试匹配默认协议；工作台section两阶段SDK已有1604独立覆盖。DshProjectStages 2/2 /tmp/WorkbenchMergedDshStages.log。受控SSE真实DSH SDK完整7角色Run通过 /tmp/WorkbenchMergedFlowAcceptance.log（该log总3中先2失败为旧checkpoint key）。AutomatedLanggraphFlow原有产物/修订/门禁断言只更新contract-v10到v11，重跑2/2 /tmp/WorkbenchMergedLanggraphFlow.log。固定C++编译运行、第二轮修订、内部Worker复用、发布和审计均执行；不能称真实供应商或jsmn/TinyXML2验收。

Architecture/Site采用main与当前AGENTS一致的行为/依赖检查，移除已由main废弃的文案/内部文件布局/实现语法断言；保留feature外层必须经Domain进入角色执行、确定性association/evaluation不得调用生成角色的断言（更新当前路径），保留Pages Actions源/权限/条件部署检查，以及frame-ancestors none限制。/tmp/WorkbenchMergedArchitectureSite.log 9/9通过。ComponentLayout仍U未决定，main删除而feature有旧文件布局/文案/源码字符串检查，需按当前验证治理审视，不可盲目保留过期路径。

全树npm run typecheck起初两错误：FixtureProjectScenario要求TS DSL夹具提供native testSource/testPath；KnowledgeRoleFixture引用已删除taskDependencies。现将fixture assets明确union为native源码或显式testSuite（互斥），native运行前缺testPath/testSource明确拒绝；移除没有调用者的旧knowledgePlan函数，仅保留outline/sections helper。/tmp/WorkbenchMergedAllTypes.log最终exit0，全树类型通过。此两文件改动尚需stage。git diff --check定向通过。

后运行完整TypeScript ModuleFlywheel回归 /tmp/WorkbenchMergedModuleFlow.log，终态exit1，7项中1通过6失败：错误候选预期拒绝晋升通过，其余主要DOCGEN_WORKER_EXECUTOR_MISSING。测试dependencies仍按外层DocWorker旧机制，未传workerRuntime；可参考已通过AutomatedLanggraphFlow传prompts/observer/ConcurrentTasks。不能只改预期掩盖问题：此测试显式断言DocGen outline/body、Worker source-facts反馈、H2修订、知识风险、来源不泄露和固定门禁，generic TS应用当前buildAgentCommand默认main DocGen/Review，与工作台模式期望可能不一致。应先明确按TS moduleContract接入显式section/source-facts及工作台修订路径，或将测试通过真实工作台应用验证同等行为；不要丢上述安全/行为断言。保留C++默认main协议已通过。首先读ModuleFlywheel与AutomatedProjectWorkflow的DocGen/Review/recordGateDecision，再补内部Worker runtime并定向排查。

所有本轮测试session均终态，无本任务活跃模型/编译进程。真实模型运行、前台完整交互、C/C++最终source质量/关联/发布与新版部署仍待做。没有改旧证据/可信测试预期，也未削减隔离/资源阈值。
