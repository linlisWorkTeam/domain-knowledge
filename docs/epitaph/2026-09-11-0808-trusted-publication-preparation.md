# 可信复核接入发布准备

/tmp/domain-knowledge-workbench，feat/five-stage-workbench，基线55c21441000209fad5042e1928c5fe3894958b0f。原脏区/旧网站不变，未删知识，目标active；Node24/384MiB重任务串行。

WorkbenchPublicationEvidence.prepare已增加必须的tests:Pick<NativeTestStore,get>端口，重读并冻结可信集，纳入递归CAS扫描及准备凭据。校验项目快照/源码版本、稳定卡片与正文摘要、工具链digest/ref、报告归属与计数；参考manifest摘要对set.binding，生成manifest摘要对report，生成language/逐文件摘要对Code。调用NativeTrustedPublication逐案复算；同步克隆fixedSuites防异步期间输入变更。准备契约v2，PREPARED/publicationVerified=false。

10定向单元/集成、8架构、类型/Spec通过，反例包括新合法生成manifest摘要冒充另一语言、正文/工具链绑定替换。证据publication-trusted-preparation。尚未真实完成prepare成功：完整来源未通过；此前单独真实C31/CPP37原始可信报告已验证。服务仍未接Composition/API/UI。

下一步：增加持久化project快照端口，核验固定manifestRef/参考manifest文件/构建参数、可信interface/policy绑定；生成manifest构建约束也须匹配项目（当前只比语言与文件）。加载源码接口后重新验证suite schema。来源校验需按卡片正文全部H2复算每章原始Review与冻结criteria/result，不能只信汇总。之后执行独立SQLite发布审计/可恢复Markdown事务并接入一键及分步发布入口。勿把PREPARED称作VERIFIED。

来源task stage-3ac5a7c55407b750e1205020b04657f09b5e4d4590903f6e5592bf3ab3967b48，PID2411792/session82991再次确认活跃，最新RUNNING，16章节/1卡片。driver /tmp/RunWholeSourcePolicyV1.ts，日志 /tmp/RealWholeSourcePolicyV1.log，完整证据source-policy-v1/WholeSource.json。没有新增重模型任务，保持原任务运行，未把观察等待当终止。

完整剩余目标仍为真实来源修订后新重建/可信固定来源复测、双目标新版一键完整链路、最终发布、Make/CMake模块参数、TS完整分析/历史选择、全回归与网站部署。
