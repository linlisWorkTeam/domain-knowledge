# 角色工作区清单进入删除组合

目标active。独立/tmp/domain-knowledge-workbench，线上仍d869e1a。未部署/删用户数据/调用模型；PR50 Draft，不合并38/50。

WorkspaceDeletionManifest读取受保护审计里的workspaceRoot+metadata.runId，匹配库存runs/wb_stage_tasks，再从.flywheel-workspace.json提取明确文件。只接受32hex角色目录或stage-materials/64hex；后者核对idempotencyKey摘要且必须无复制源码。兼容deepseek-harness-sdk和headless。归属共享由领域保留，未知共同所有者生成configuration保护引用；完全没有当前执行所有者的旧审计跳过目录，不自动扫孤儿。manifest被纳入文件清理，未知额外文件保留。

工作区文件复用PublishedDeletionFile及published-files见证命名空间，以workspaces根名区分；SqliteDeletionSnapshot.published.additional在每次扫描追加文件及保护节点，SqliteRuntimeDeletions增加可选workspaces(rootNames,audits)。publicationRoots需要显式含工作区根且不能与其他清理根重叠。未配置时仍无工作区清理，不应直接开放全文件删除。

CasDeletionReader保持所有CAS已知摘要验证，新protected inspectRelative仅用于第一次读可信服务器清单，仍用逐层O_NOFOLLOW/pinned fd/普通文件/体积/读取前后核对；清理时继续核对冻结摘要。readWorkspaceAudits只读runtime/demo/agent-runs.jsonl和workbench-audit/model.jsonl，各16MiB、合计10万条，缺失允许，损坏或链接拒绝；仍要在维护排他下使用。审计与用量记录不删除。

WorkspaceDeletionTests.log29/29：真实LocalAgentWorkspace产物清理、源码和额外文件保留、同见证重试、未知/已知共享所有者保护、阶段身份键、旧headless、越界/链接/损坏审计；原CAS/发布文件/恢复/运行组合/架构回归。WorkspaceDeletionTypes.log和WorkspaceDeletionSpecs.log通过。

未完成：公共Composition维护会话与HTTP/UI接线、启动恢复后队列；CodeAgent sessions（sha256(idempotencyKey).json可从审计归属）、DSH随机临时home（idempotencyKey前24摘要+UUID，可按审计关联但缺中途失败记录处理）、保留evaluationArtifactsDirectory及其他本地文件归属。DSH审计只存workspace不存home；不可简单按目录名删无归属文件。缺审计的角色视图也未自动处理。线上数据只读备份可继续审计，不能删除当前用户数据。C/C++来源质量/修订后的全部门禁/最终发布仍未完成。
