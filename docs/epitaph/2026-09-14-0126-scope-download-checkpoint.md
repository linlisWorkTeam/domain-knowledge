# 修复运行中构建范围下载

实际工作树/tmp/domain-knowledge-workbench，基线112ba4c，先读0122/确认Git。上轮页面实现和3测试是progress。本轮追踪真实下载路径：Server stage-artifacts→WorkbenchReconstruction.artifact只允许result/checkpoint/event绑定refs，不允许任意input ref。a027先前只在最终result绑定executionScopesRef，112ba页面会在运行中提前404并缓存失败，发现实际集成缺口。

已先写Spec并修复：新scope策略verify在可信oracle/项目/CAS范围复验之后立即保存source-execution-scope检查点，绑定scope清单及全部reference/fingerprint refs，Review调用前即可下载；旧策略不增加新检查点。页面只有检测scope ref属于检查点/最终结果时才拉取，未完成运行校验显示等待原因且不提供必然404的下载。保留原工件授权边界，不开放所有输入或任意CAS。

新增SourceExecutionDownloadHttp.test.ts实际免登录HTTP+SQLite/CAS：input ref alone404→claim/checkpoint后RUNNING时200→无关ref404→失败仍200。SourceExecutionPanel增加waiting情形断言不发提前请求。5/5（/tmp/SourceScopeDownloadTests.log，2557ms）、typecheck/Spec/diff通过。WorkbenchEvaluation的受控模型增加断言Review开始前已有scope检查点；其重原生回归仍由既有队列等待，不能称已通过。此次未运行浏览器、未部署。

当前真实source仍RUNNING：stage-54a2931f73371a18fc102c4cc723adbe65c75a66f4842f80351af3bc46061de5，session69756/PID3596179，/tmp/SourceAfterTargets.log、source-after-target-policy/Source.json。本轮末ps Sl/live20分41秒，12calls221132tokens/reserved1021518/elapsed1230001ms；第12调用仍进行，单次600秒约束保持，不因暂无token更新重启。当前source仍旧冻结scope策略，未注入新实现。已有31/31可信及11/11固定，尚未整卡来源通过/发布。

回归队列session20697/PID3605442依然ps Ss/live8分43秒，/tmp/RunScopeApplicationAfterSource.py按源PID及start=1641798545退出后才运行WorkbenchEvaluation.test.ts，日志/tmp/SourceScopeApplicationTests.log；不要重复启动或抢先并行原生/全回归/浏览器。下一轮先查两个handle/同PID是否结束，源终态后读取全部结果并等队列完成，若测试失败保留日志并修复。

之后继续真实来源修订/补证、新scope策略的实际模型证据、TinyXML2 XMLUtil当前完整链路、一键/逐步、当前完整Node/Console/MarkdownLite/截图/最终部署。线上仍dc3dbf8。此任务只修运行中下载交接，不代替原完整验收，goal继续。
